apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "job-store.fullname" . }}
  labels:
    {{- include "job-store.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  # Recreate, never RollingUpdate: two pods would mean two SQLite writers and
  # corrupt the DB. Do not change without moving off SQLite.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "job-store.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "job-store.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 999
        # fsGroup so the persistence PVC is group-writable by the runtime user;
        # harmless for the emptyDir fallback.
        fsGroup: 999
        seccompProfile:
          type: RuntimeDefault
      {{- with .Values.initContainers }}
      initContainers:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: job-store
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          ports:
            - name: http
              containerPort: 5000
          env:
            # DB on the writable /data mount (a PVC by default; emptyDir when
            # persistence is disabled). See persistence.* in values.yaml.
            - name: JOBS_DB_PATH
              value: /data/jobs.db
            # HOME on writable /tmp so gunicorn's control socket and worker
            # heartbeat files don't hit the read-only root filesystem.
            - name: HOME
              value: /tmp
            # API key (always a Secret). The resume is read from RESUME_PATH —
            # by default a file projected from the Secret (resume.fromSecret),
            # but an operator can source it elsewhere (e.g. an initContainer
            # git-clone into an extraVolume) by setting resume.fromSecret=false.
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "job-store.secretName" . }}
                  key: anthropic-api-key
            - name: RESUME_PATH
              value: {{ .Values.resume.path | quote }}
            {{- with .Values.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: {{ .Values.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.liveness.periodSeconds }}
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: {{ .Values.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.readiness.periodSeconds }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: data
              mountPath: /data
            - name: tmp
              mountPath: /tmp
            {{- if .Values.resume.fromSecret }}
            - name: resume
              mountPath: {{ dir .Values.resume.path | quote }}
              readOnly: true
            {{- end }}
            {{- with .Values.extraVolumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
        {{- with .Values.extraContainers }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      volumes:
        # jobs.db (+ WAL/SHM). Backed by a PVC so state survives pod restarts;
        # emptyDir (disposable) when persistence is disabled. fsGroup (above)
        # makes the mount writable by the non-root runtime user.
        - name: data
          {{- if .Values.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ include "job-store.fullname" . }}-data
          {{- else }}
          emptyDir: {}
          {{- end }}
        # Writable scratch required because the root filesystem is read-only:
        # gunicorn worker heartbeat + control socket (via HOME=/tmp) and tmpfiles.
        - name: tmp
          emptyDir: {}
        {{- if .Values.resume.fromSecret }}
        # Resume YAML projected from the Secret as a single read-only file at
        # resume.path (the anthropic key is env-only, not a file).
        - name: resume
          secret:
            secretName: {{ include "job-store.secretName" . }}
            items:
              - key: resume.yaml
                path: {{ base .Values.resume.path | quote }}
        {{- end }}
        {{- with .Values.extraVolumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
